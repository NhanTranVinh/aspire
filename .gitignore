# .NET Core and .NET 5+
## Ignore Visual Studio temporary files, build results, and
## files generated by popular Visual Studio add-ons.
##
## Get latest from https://github.com/github/gitignore/blob/main/DotNET.gitignore

# User-specific files
*.rsuser
*.suo
*.user
*.userosscache
*.sln.docstates

# User-specific files (MonoDevelop/Xamarin Studio)
*.userprefs

# Build results
[Dd]ebug/
[Dd]ebugPublic/
[Rr]elease/
[Rr]eleases/
x64/
x86/
[Ww][Ii][Nn]32/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
bld/
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/

# Visual Studio Code files
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# Rider files
.idea/
*.idea.*
*.iml

# ASP.NET Scaffolding
**/ScaffoldingReadMe.txt

# Visual Studio cache files
.vs/
# Blend temporary files
.blenderconfig

# Visual Studio 2019 cache/options
.editorconfig

# NUnit / xUnit / MSTest test results
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.*
*.VisualState.xml
TestResult.xml
*.trx
*.coverage
*.coveragexml

# ReSharper is a Visual Studio extension that provides on-the-fly code analysis
# and real-time error highlighting.
# Ignore all ReSharper cache files, settings locally stored, and log files.
_ReSharper*/
*.[Rr]e[Ss]harper
*.DotSettings.user

# TeamCity is a build management and continuous integration server.
# Ignore TeamCity NUnit test results.
*-Lab.xml

# DotCover is a code coverage tool.
*.dotCover

# dotTrace performance snapshot files
*.dtp
*.dtk

# Paket dependency manager
.paket/paket.exe
paket-files/

# FAKE - F# Make
.fake/

# Cake - C# Make
tools/cake.exe

# PowerShell module builds
ps_modules/

# Stores files used by Visual Studio to customize Web server behaviour.
# Exclude the IIS Express ApplicationHost.config file.
**/App_Data/
**/[Aa]pp_[Dd]ata/

# Output folder from the `dotnet publish` command
**/publish/

# NuGet Packages
# Exclude packages folder, generally it's better to restore them.
# However, in some cases you might want to check them in (e.g. if you can't restore them)
*.nupkg
# NuGet v3 Global Package Cache
# Windows: %userprofile%\.nuget\packages
# Mac/Linux: ~/.nuget/packages
.nuget/packages/
# NuGet temporary folder
.nuget/v3-cache/
# Ignore packages folder for solutions using packages.config (legacy)
**/packages/*
!**/packages/repositories.config

# Migration User Files
# Might contain sensitive information
*-MigrationHistory.txt

# Local environment settings
# These files often contain connection strings or other sensitive data
# and should not be committed. Use User Secrets or environment variables instead.
appsettings.Development.json
secrets.json
*.env
.env.*
!.env.example

# Secrets Path
# Generally, an absolute path to User Secrets should not be committed
# This is a common pattern though:
**/secrets/

# Windows image file caches
Thumbs.db
ehthumbs.db
ehthumbs_vista.db

# Dump files
*.stackdump
*.mdmp

# Visual C++ cache files
ipch/
*.aps
*.ncb
*.opensdf
*.sdf
*.cachefile
*.VC.db
*.VC.VC.opendb

# Node.js build outputs
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # Often committed, but can be ignored if using only yarn.lock or similar
yarn.lock # Often committed, but can be ignored if using only package-lock.json or similar
dist/
coverage/ # JavaScript test coverage

# Tools
# Ignore any local dotsettings files
*.DotSettings

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Linux
*~

# If you're using Docker
Dockerfile
.dockerignore
docker-compose.yml
docker-compose.override.yml
# Add any other Docker-related files you don't want to commit

# Local History for JetBrains products
.idea/shelf/
.idea/workspace.xml
# Ignore local history folder from Rider (specific to Rider)
.idea/.idea.*/**/localHistory/

# Specific to API projects
# Swagger/OpenAPI generated files that might be regenerated (optional)
# If you generate clients or SDKs from your swagger spec, you might want to ignore them
# if they are always re-generated as part of the build.
# swagger.json # Often committed as the source of truth, but can be ignored if generated.
# swagger.yaml # Same as above.

# Log files from application runs
*.log
logs/
*.log.*

# Entity Framework Core Migrations design-time data
# These are temporary and should not be checked in.
# Typically found in obj/Debug or obj/Release folders which are already ignored,
# but explicit ignore can be useful.
*.__EFMigrationsHistory.*

# BenchmarkDotNet
BenchmarkDotNet.Artifacts/

# Coverlet coverage results
coverage.json
coverage.cobertura.xml
coverage.opencover.xml